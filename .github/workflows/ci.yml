name: CI

on:
  push:
    branches:
      - main

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run commands
        run: |
          echo "CI настроен."
          ls -la

      - name: Build an image.
        run: |
          docker build -t my-test-image ./backend
